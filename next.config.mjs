/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["www.themealdb.com"], // Thêm tên miền của hình ảnh
    },
  };
  
  export default nextConfig; // Sử dụng export default thay vì module.exports